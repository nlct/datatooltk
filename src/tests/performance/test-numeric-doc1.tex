% arara: pdflatex
\documentclass{article}

\usepackage{datatool}

\DTLsetup{default-name=testdata}
\DTLread[format=csv,csv-content=tex]{numeric-data.csv}

\begin{document}
Aggregates.

\DTLaction[column=1,options={sd,min,max}]{aggregate}

Count: \DTLuse{count}.
Min: \DTLuse{min}.
Max: \DTLuse{max}.
Sum: \DTLuse{sum}.
Mean: \DTLuse{mean}.
Variance: \DTLuse{variance}.
SD: \DTLuse{sd}.
\end{document}
